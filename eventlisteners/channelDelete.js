/**
 * Detect when channel is deleted
 * @param {*} client 
 * @param {*} config 
 */
 function channelDelete(client) {
    //message event listener
    client.on('channelDelete', channel => {
        
    })
 }